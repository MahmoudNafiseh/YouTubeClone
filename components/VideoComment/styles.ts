import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  avatar: {
    marginHorizontal: 10,
  },
  commentRowView: {
    flexDirection: "row",
    alignItems: "center",
    paddingBottom: 10,
  },
  commentRowText: {
    fontSize: 14,
    paddingRight: 40,
  },
});

export default styles;
